version_base: 1.1
defaults:
  - _self_
  - model: afgsa
  - trainer: default
  - data: default

seed: 990819

paths:
  root: ${hydra:runtime.cwd}
  out_dir: ${hydra:run.dir}

hydra:
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  job:
    chdir: false
  sweep:
    dir: multirun/${now:%Y-%m-%d}
